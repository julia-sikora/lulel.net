{% extends 'base.html.twig' %}

{% block title %}{{ "filament.title_edit" | trans }}{% endblock %}

{% block body %}
    <h1>{{ "filament.title_edit" | trans }}</h1>

    {{ include('filament/_form.html.twig') }}

    <a href="{{ path('app_filament_index') }}">{{ "filament.button.back_to_list" | trans }}</a>

    {{ include('filament/_delete_form.html.twig') }}
{% endblock %}
