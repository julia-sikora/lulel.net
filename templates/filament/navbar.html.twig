<header>
    <nav class="navbar navbar-dark bg-dark navbar-expand-lg">
        <a class="navbar-brand" href="{{ path('app_filament_index') }}"><img src="{{ asset('logo.png') }}" width="30" alt="" class="d-inline-block me-1 ms-1 w-5 align-text-top">
            <text class="d-none d-md-inline-block">{{ "filament.navbar.filament" | trans }}</text>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainmenu">
            <span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="mainmenu">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link disabled" href="{{ path('app_filament_index') }}">{{ 'filament.navbar.profile'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_filament_new') }}">{{ 'filament.navbar.create_new'|trans }}</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" role="button">{{ 'filament.navbar.change_language'|trans }}</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params') | merge({'_locale': "en"}))}}">ENG</a>
                        <a class="dropdown-item" href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params') | merge({'_locale': "de"})) }}">DE</a>
                        <a class="dropdown-item" href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params') | merge({'_locale': "pl"})) }}">PL</a>
                    </div>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_homepage') }}">{{ 'filament.navbar.back_to_homepage'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link disabled" href="{{ path('app_filament_index') }}">{{ 'filament.navbar.contact'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">{{ 'filament.navbar.logout'|trans }}</a>
                </li>
            </ul>
            <form class="d-flex ms-auto m-1">
                <input class="form-control m-1 mb-3 mt-3" type="search" placeholder="Search">
                <button class="btn btn-outline-success bg-dark button" id="search-button" type="submit">Search</button>
            </form>
        </div>
    </nav>
</header>